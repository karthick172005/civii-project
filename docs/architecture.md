Architecture Overview
- Mobile (Flutter) → Backend (Node) → Storage (Firebase Storage) → DB (Firestore)
- AI (Python) called by Backend for category & NSFW checks
- Admin dashboard queries DB and approves reports (sends FCM + emails)

Diagram: docs/architecture.png  <-- to be added Day 2/3
